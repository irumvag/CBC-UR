# Supabase Configuration
# Get these values from your Supabase project settings:
# https://app.supabase.com/project/_/settings/api

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Site URL (used for admin authentication)
# Set this to your production URL, e.g., https://cbc-ur.vercel.app
VITE_SITE_URL=https://your-site-url.com

# Note: The app will work with mock data if Supabase is not configured.
# This allows for frontend-only development without a Supabase backend.

# ============================================
# Admin Setup
# ============================================
#
# Admins are added directly to the system (no public signup).
# To add an admin:
# 1. Create a user in Supabase Authentication dashboard
# 2. Add a corresponding row in the 'members' table with role='admin'
#
# The admin panel is accessible at /admin after sign-in.
